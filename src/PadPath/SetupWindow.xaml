<Window x:Class="PadPath.SetupWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set up PadPath" Width="820" Height="650" MinWidth="680" MinHeight="560" WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextBrush}" FontFamily="Segoe UI Variable Display, Segoe UI">
    <Window.Resources><Style TargetType="CheckBox"><Setter Property="Foreground" Value="{DynamicResource TextBrush}"/></Style></Window.Resources>
    <Grid Margin="34">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel><TextBlock Text="Set up your game folders" FontSize="32" FontWeight="Bold"/><TextBlock Text="Pick one or more places to browse. You can change these later with Y or F2." Margin="0,8,0,0" FontSize="16" Foreground="{DynamicResource MutedBrush}"/></StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,24,0,14">
            <Button Content="＋ Add folder" Click="AddFolder_Click" Padding="18,10" FontSize="17" Margin="0,0,10,0"/>
            <Button Content="Remove selected" Click="RemoveFolder_Click" Padding="18,10" FontSize="17"/>
        </StackPanel>
        <ListBox x:Name="FolderList" Grid.Row="2" Background="{DynamicResource PanelBrush}" Foreground="{DynamicResource TextBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="2" FontSize="18">
            <ListBox.ItemTemplate><DataTemplate><Grid Margin="10"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions><TextBlock Text="{Binding Name}" Foreground="{DynamicResource TextBrush}" FontWeight="Bold"/><TextBlock Grid.Column="1" Text="{Binding Path}" Foreground="{DynamicResource MutedBrush}" TextTrimming="CharacterEllipsis"/></Grid></DataTemplate></ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Row="3" Margin="0,16,0,8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="270"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="170"/></Grid.ColumnDefinitions>
            <TextBlock Text="Color palette" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,14,0"/>
            <ComboBox x:Name="ThemeCombo" Grid.Column="1" FontSize="17" Padding="10,7" SelectionChanged="ThemeCombo_SelectionChanged">
                <ComboBox.ItemTemplate><DataTemplate><StackPanel Orientation="Horizontal"><Rectangle Width="18" Height="18" Fill="{Binding Preview1}"/><Rectangle Width="18" Height="18" Fill="{Binding Preview2}"/><Rectangle Width="18" Height="18" Fill="{Binding Preview3}" Margin="0,0,9,0"/><TextBlock Text="{Binding Name}" VerticalAlignment="Center"/></StackPanel></DataTemplate></ComboBox.ItemTemplate>
            </ComboBox>
            <TextBlock Grid.Column="2" Text="Appearance" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" Margin="22,0,10,0"/>
            <ComboBox x:Name="AppearanceCombo" Grid.Column="3" FontSize="17" Padding="10,7" SelectionChanged="AppearanceCombo_SelectionChanged"/>
        </Grid>
        <WrapPanel Grid.Row="4" Margin="0,10,0,18">
            <CheckBox x:Name="ConfirmCheck" Content="Confirm before launch" Margin="0,0,24,10" FontSize="16"/>
            <CheckBox x:Name="HiddenCheck" Content="Show hidden files" Margin="0,0,24,10" FontSize="16"/>
        </WrapPanel>
        <Grid Grid.Row="5"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <Button x:Name="SteamButton" Content="Add to Steam" Click="SteamButton_Click" Padding="20,12" FontSize="17" HorizontalAlignment="Left"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal"><Button Content="Cancel" Click="Cancel_Click" Padding="20,12" FontSize="17" Margin="0,0,10,0"/><Button Content="Save and continue" Click="Save_Click" Padding="20,12" FontSize="17" FontWeight="Bold" Background="{DynamicResource AccentBrush}" Foreground="{DynamicResource AccentTextBrush}"/></StackPanel>
        </Grid>
    </Grid>
</Window>
